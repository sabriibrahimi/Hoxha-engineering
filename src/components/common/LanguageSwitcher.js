// Temporary file to fix import error - this will be replaced by LanguageSwitcherFixed
import LanguageSwitcherFixed from './LanguageSwitcherFixed';

const LanguageSwitcher = (props) => {
  return <LanguageSwitcherFixed {...props} />;
};

export default LanguageSwitcher;
